/**
 * Approach: 
    Find the largest element. then do a prefix sort, then reverse the array.
    Find the largest element in array of size n - 1 (largest is stored at correct position), and continue the previous step. 
 */
